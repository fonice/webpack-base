<%
const template = require('../../layout/template.js')

const content = require('./test.ejs');  // 调取存放本页面实际内容的模板文件
// content({name:'man'})
// const html = content.run();
// const layout = require('../layout.js');  // 调用管理后台内部所使用的布局方案，我在webpack配置里定义其别名为'layout'
template.pageTitle='这是首页';
template.content=content({name:'man'});
var html = template.run()
// var a=require('../../css/common.css');
// 给layout传入“页面名称”这一参数（当然有需要的话也可以传入其它参数），同时也传入页面实际内容的HTML字符串。content({ pageTitle })的意思就是把pageTitle作为模板变量传给ejs模板引擎并返回最终生成的HTML字符串。
// var html = template.run();

%>
<%=html%>
